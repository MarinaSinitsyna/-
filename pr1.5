#include <stdio.h>
#include <string.h>
int main ()
{
	char arr[50];
	printf("enter the name of the movie\n");
	scanf("%s",&arr);
	printf("%45.50s\n",arr); //Спецификация определяет печать массива в поле шириной не более 45 позиций и размером в 50 символов 

	return 0;
} 
